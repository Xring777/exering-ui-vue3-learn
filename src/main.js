import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
// import ExeringUI from '../modules/exering-ui'
// import Transfer from '../modules/exering-ui/Transfer'

// createApp(App).use(Transfer).mount('#app')
createApp(App).mount('#app')
